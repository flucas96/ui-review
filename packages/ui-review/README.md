# ui-review

Local-first visual feedback for HTML and framework-based web applications. Run `ui-review <url-or-path>` to open an annotated review proxy, or `ui-review mcp --root .` to expose its feedback to an MCP-compatible coding agent.

See the repository README for the complete setup, SSH workflow, and Claude Code integration.
